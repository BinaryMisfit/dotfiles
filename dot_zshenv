# User-local bin first
export PATH="$HOME/.local/bin:$PATH"

# Locale (safe defaults)
export LANG="${LANG:-en_GB.UTF-8}"

# Copilot CLI Modular Instructions
export COPILOT_CUSTOM_INSTRUCTIONS_DIRS="${COPILOT_CUSTOM_INSTRUCTIONS_DIRS:-$HOME/.copilot/instructions}"

# Copilot CLI Experiments
export COPILOT_CLI_EXPERIMENTS="${COPILOT_CLI_EXPERIMENTS:-PERSISTED_PERMISSIONS,SESSION_CLEANUP,KEEP_ALIVE,SUBAGENT_COMPACTION,SESSION_STORE,STATUS_LINE,SHOW_FILE,ASK_USER_ELICITATION,MULTI_TURN_AGENTS,EXTENSIONS,CONFIGURE_COPILOT_AGENT,OPUS_MEDIUM_EFFORT_DEFAULT,BACKGROUND_SESSIONS,RUBBER_DUCK_AGENT}"
