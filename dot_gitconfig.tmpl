[user]
    name = {{ .git.name }}
    email = {{ .git.email }}

[core]
    editor = nvim
    autocrlf = input

[pull]
    rebase = false

[init]
    defaultBranch = main

[push]
    autoSetupRemote = true

[credential]
    helper = manager-core
    useHttpPath = true
