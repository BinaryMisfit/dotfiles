# Todo Register — retired

**Retired 2026-09-23, BinaryMisfit's own direct call in `#the-forge`.** This repo's open
items are now live in the `afterglow-todo` MCP service (`project: "binary-dotfiles"`) — real,
searchable via `ask`, no longer tracked here. All 6 real open items were verified present
via `list_items` before this file was retired.

**Full prior content kept in this file's own git history, not deleted** — if this repo ever
needs the paper register back, `git log -p -- docs/todo-register.md` has it in full.

Going forward: use `mcp__afterglow-todo__list_items` / `add_todo` for this repo's open work,
not this file.
